// wifi_config.h.example — copy this to wifi_config.h and fill in your own
// network. wifi_config.h itself is gitignored and never committed.
//
// WiFi is used ONLY to set the clock via NTP (there is no cloud upload).
#pragma once

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"
